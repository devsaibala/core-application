name: release

on:
  workflow_dispatch:
    inputs:
      app:
        type: boolean
        description: Inclcue App
       

jobs:
  app-deploy:
     if: ${{ github.event.inputs.app }} == true
     runs-on: ubuntu-latest
     steps:
      - name: Check out code
        uses: actions/checkout@v2
        
      - name: deploy App
        run: |
         echo " runing App deployment now "
          
